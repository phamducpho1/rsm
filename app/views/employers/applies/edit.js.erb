$('#apply-handling-content').html('<%= j (render partial: "employers/applies/appointment_form", locals: {apply: @apply, members: @members, appointments: @appointments, is_interview_scheduled: @is_interview_scheduled}) %>');
